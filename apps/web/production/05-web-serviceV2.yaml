# SERVICE version 2
apiVersion: v1
kind: Service
metadata:
  name: web-v2-service
  namespace: final-project
spec:
  selector:
    app: web-v2
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80